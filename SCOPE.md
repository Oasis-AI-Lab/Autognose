# Autognose 范围与技术栈（Scope & Stack）

> 依据 `AGENT.md` §4 与 §2 协作协议产出：**不含代码、不建目录树、不加依赖**。
> 全部建议标注为**建议（非决策）**。
> 标记约定：`[FACT]` 有据可查 · `[JUDGMENT]` 有推理链 · `[ASSUMPTION]` 待验证 · `[OPEN]` 需团队裁决。

---

## §0 现状核对

### 0.1 仓库文件（截至 2026-09-12）

| 文件 | 性质 | 状态 |
|---|---|---|
| `README.md` | 2 行定位句 | 已提交 |
| `LICENSE` | MIT, Copyright 2026 CeaserZhao | 已提交 |
| `AGENT.md` | 原则 + 边界 + 协作协议（111 行） | 已提交 |
| `THINKING.md` | 四层思维模型 L0–L3（277 行） | 已提交 |
| `site/index.md` | 定位页，5 节，569 词，内嵌 `<style>` | 已提交 |
| `index.html` | 中文调研报告，3555 行 / 257 KB，零外部依赖 | 已提交 |
| `.nojekyll` | GitHub Pages 直发标记 | 已提交 |
| `BOUNDARY_REPORT.md` | D1–D6 待决清单 + 选项权衡 | **未跟踪** |
| `SCOPE.md` | 本文件 | **未跟踪** |
| `prepare/`、`.workbuddy/` | 图片与记忆 | 被 `.gitignore` 忽略 |

`[FACT]` 调研报告已由 `self-aware-knowledge-base.html` 改名为 `index.html` 并提交，`site/index.md` 亦已提交。**代码现状**：零实现代码、零测试、零构建配置、零依赖清单、无 CI。

### 0.2 文档完整性问题

1. `[FACT]` `THINKING.md` 第 5 行与第 270 行要求读 `WORKFLOW.md`——**该文件不存在**（承担协作协议的是 `AGENT.md` §2）。
2. `[FACT]` `.workbuddy/memory/2026-09-12.md` 称 `AGENT.md` 为 "AGENTS.md"——文件名不一致。
3. `[OPEN]` `BOUNDARY_REPORT.md` 与 `SCOPE.md` 两份决策文件未进版本控制。调研报告已解决此问题，这两份仍未。

---

## §1 首要待决项：薄位还是厚位

### 1.1 判据：状态存在谁那里

- **薄库**：状态在调用方。无状态函数，状态随调用进出。不碰模型、不碰存储、不碰管线。
- **厚框架**：状态在 Autognose 里。自带存储、自带索引、自调模型、自成运行时。

| 维度 | 薄库 | 厚框架 |
|---|---|---|
| 存储 | 无，状态由调用方传 | 必须有，schema 迁移/并发/损坏恢复全自担 |
| 检索 | 不管，任何检索器皆可 | 必须有，等于"不重复造轮子"作废 |
| 依赖方向 | Mem0/Letta/LangChain **引用你** | 与 Mem0/Letta **互相替代** |
| 每期可交付 | 判决函数 + 指标 | 先交付存储与抽取管线，判决最后才出现 |
| 可单测性 | 纯函数，输入输出全可控 | 需起存储喂数据，任何 bug 都可能污染指标 |
| 成功标准 | 别人**引用**你 | 别人**使用**你的产品 |

### 1.2 两条决定性差异

**（甲）厚框架无法分段验收，薄库可以。** `AGENT.md` §5 铁律 2 是"评价优先"，而评价的前提是能单独测。薄库天然切成两段：单元层（构造证据 → 检查动作，**纯合成数据即可**）；整机层（真实语料上的弃权 precision/recall，**需外部标注**）。团队已确认暂无标注，薄库允许先做扎实第一层并发布。厚框架做不到——存储、抽取、判决任一处错，指标坏了无法定位。

**（乙）厚框架过不了报告自己的入场券。** 报告 §1.3 的判据是"能否问它'你在这个主题上知道多少'并得到有信息量的回答"。薄库的答案由调用方传入的元数据算出，结构上必然可答；厚框架必须先灌入用户**全部**知识才答得出，而报告判定人工元数据维护"没人会做"（§4.3）。且 `site/index.md` §3 已公开承诺"低开销"，厚框架天然反这条。

### 1.3 报告自身的取向（两种读法都能成立）

- **薄边证据**：图 6-1 组件为虚线贯穿层；图 2-1 明说"不自带知识，也不自己做检索"；§2.5 把八个邻居全部标为"被增强方 / 承载方 / 上游供给 / 理论母体"——**无一为竞争方**；§4.3 引述"这不是研究问题，是工程问题……我们只需要把它建出来"。
- **厚边证据**：§6.2 蓝图 C1–C8 八个组件、C7 治理层、C8 评价层全在框内；图 4-1 的"理想中的自我感知知识库"为六维全 ●。

`[JUDGMENT]` 报告描述"整机"，但给出的**可执行子集是薄的**：§6.2 折叠块《如果只做三件事》——来源标签、弃权契约、过时标记——**没有一件需要自己拥有存储**。

### 1.4 薄位的代价

`[FACT]` 报告原话："这不是研究问题，是工程问题。" 低门槛在诚实性上站得住，**在防御性上站不住**——LangChain 加 provenance 插件、Mem0 补 abstain 信号，都做得到。

`[JUDGMENT]` 薄位唯一的护城河是**成为被引用的标准**。标准只能由薄的东西承担：厚框架的接口是产品，薄库的接口是**协议**。报告 §6.2 图注已写明这一点——"八个组件必须共享同一套知识状态表示（C1），否则退化为八个互不通话的独立模块"，共享表示即标准。

> **`[OPEN]` 待裁决：薄位还是厚位。** 这一条定了，字段集范围、依赖策略、是否碰存储全部随之确定。两个成功标准（被别人引用 / 被别人使用）不能同时最大化。

---

## §2 生态位（报告原文级）

### 2.1 位置

`[FACT]` §2.1 / §2.2：

> 自我感知知识库**不是一个独立赛道**，而是横跨在"知识层"和"应用层"之间的一个**元层**：向上给智能体提供"我现在靠不靠得住"的判断，向下要求知识层提供来源与时效元数据。

> 在知识管理生态里，它占据的是**治理层**：它不生产内容，不检索内容，而是对内容的状态进行建模、审计与仲裁。这与数据世界里**数据目录（Data Catalog）的位置完全对应**，区别在于它面向的是非结构化知识，且必须支持机器消费。

四个数据点：不自带知识 · 不自己做检索 · 消费知识层的元数据 · 中介检索层的结果。

### 2.2 判据：输出是排序还是判决

`[FACT]` §2.5：

> 更聪明的 RAG 仍在回答"哪条知识最相关"；自我感知知识库回答的是"这一批证据够不够格被用来回答这个问题，如果不够格，我该做什么"。**前者的输出是排序，后者的输出是判决——包括"不回答"这个判决。**

入场测试（§1.3）：能否向它提问"你在这个主题上知道多少"并得到有信息量的、非套话的回答。

### 2.3 为什么单层框架填不了这个位置

`[FACT]` §2.2：

> **元层必须能跨层读取信号。** 它需要来自语言模型内部状态的不确定性信号、来自检索层的证据覆盖情况、来自知识层的来源与时效标签。**任何只依赖其中一层的方案，都会退化为第二代的"智能知识库"。**

`[JUDGMENT]` LangChain 只拥有检索层、Mem0/Letta 只拥有记忆层、Ragas 只拥有评估层——各自只占一层。"横跨三层"不是框架疏忽的空地，而是**任何单层框架从内部都填不了的位置**。这是比"他们没动机"更结实的论证。

`[FACT]` 报告的硬约束（§5.6）："**白盒依赖**：SeaKR 与内部探针类方法都需要访问模型隐状态，无法直接用于闭源 API。**DH-ICL 是少数仅依赖输入输出的方案**。"

`[JUDGMENT]` 与 §2.2 直接冲突：元层理论上要跨三层读信号，工程上却只能在开源模型上读 LLM 内部状态。闭源 API 下可用信号只剩检索层 + 知识层 + 纯输入输出一致性。**这是窄切口该切在哪里的答案。**

### 2.4 产出的东西是确定性，不是知识量

`[FACT]` 图 1-1 四象限（置信度 × 实际正确），图注结论：**"自我感知知识库的全部工作：把每一格显式标注出来，并让右下→右上、左下→左上"**。

| | 低置信 | 高置信 |
|---|---|---|
| **正确** | 犹豫的真知（PSK） | 稳定已知（PAK） |
| **错误** | 明确的未知（MSU/MAU） | **过度自信的错误 · 幻觉高发区 · 最危险的一格** |

`[JUDGMENT]` 生态位上造的不是"更全的知识库"，**是确定性**——优化目标与所有邻居正交（他们优化召回与覆盖，你优化"知道自己站在哪一格"）。

### 2.5 时机：生成之前，而非之后

`[FACT]` §1.4 末尾：幻觉"本质上是一个**自我感知失败**"，"模型内部激活状态往往'知道'某陈述为假的，而生成文本却声称其为真——存在一条可被监测的内部幻觉信号"。

`[JUDGMENT]` 幻觉检测与 guardrails 都在**生成之后**干预；而这条被监测的信号必须在**生成之前**决定要不要生成。§3.3 引 Schubert 三条件亦指向同一处（自我知识须"参与与其他知识相同的推理"，即不隔离）。这是"元层"与"事后检测"的机制分界。

### 2.6 优先投入区

`[FACT]` §5.7 图 5-1，六领域按"可实现性 × 价值"定位，右上优先投入区只有两个：

- **RAG 增强**——"收益**可被精确计量**——检索次数、F1、延迟、token 成本"（DH-ICL 用一半检索次数达全量性能；SeaKR 多跳 QA 相对 IRCoT +9.5% F1）
- **企业知识管理**——"治理可先落地，与 AI 项目存活率挂钩"

医疗 / 法律在左上（价值极高、卡在责任归属与可审计性）；学术研究助手"目前更适合作为研究方向而非产品特性"（科学领域召回率估计仅 0.1%–23.2%）。

### 2.7 三条必须记录的反面证据

| 证据 | 原话 |
|---|---|
| **"克制"训不出来** | MetaCLASS：九个模型预测元认知教学动作最佳仅 **43.2%**；需沉默的 **41.7%** 场景中仅 **4.2%** 预测"不干预"——**强迫性干预偏置**。传统内容型辅导能力**不能**迁移为元认知辅导能力 |
| **是工程问题不是研究问题** | §4.3 引述："给上下文打标签、按标签过滤、让推理以来源为条件。我们只需要把它建出来" |
| **低垂果实是量出来的** | 图 4-1：F2 校准 64% / F1 边界 58% 已饱和；**F3 来源谱系 25%、F6 时效冲突 30% 最空**——"恰好是不需要算法突破、只需要工程规范的部分" |

### 2.8 报告自认的两个盲区

- `[FACT]` "**覆盖度偏重英文学术与英文工程生态**"，中文侧"可能是本报告最大的盲区"。
- `[FACT]` 附录 A.3："这个方向的文献分布在**四个几乎不互相引用的社区**里——认知科学、经典 AI 逻辑、LLM 评测、企业知识管理。术语不同，问题高度重叠，彼此不知道对方存在。"

`[JUDGMENT]` 第二条解释了图 3-2 那二十年的空档：**不是理论不够，是学科孤立。**

---

## §3 开发边界：做什么，不做什么

### 3.1 切口一句话

> `[JUDGMENT]` **Autognose v0.1 = 坐在检索系统之上、对「已有证据够不够」做判决、把弃权与追问当作一等输出、带可复现评价的最小 Python 库。**

| 限定词 | 划掉了什么 |
|---|---|
| 坐在检索系统之上 | 不碰检索、索引、向量库 |
| 对已有证据够不够做判决 | 不做生成、不做端到端问答 |
| 弃权与追问是一等输出 | 不做"总是给答案再附置信度" |
| 带可复现评价 | 不做没有指标的纯框架 |
| 最小 Python 库 | 不做服务、UI、多语言 SDK |

### 3.2 v0.1 范围内（IN）

| # | 做什么 | 依据 |
|---|---|---|
| **IN-1** | 知识状态四字段的 chunk 级标注：来源类型 / 获取时间 / 验证状态 / 任务边界 | 图 4-1 F3 覆盖度 25%（最低）；§6.1 G3"修复难度低" |
| **IN-2** | 动作与弃权契约：`answer / retrieve_more / ask / abstain / flag_review` | §6.2 C6；MetaCLASS 4.2% 沉默率 |
| **IN-3** | 判决器：四字段 + 检索分数 → 动作 + 理由 + 置信 | §2.5"输出是判决"；§2.2 元问题 |
| **IN-4** | 评价层：弃权 precision/recall 与 ECE/Brier **分开报告** | `AGENT.md` §5 铁律 2；§6.3 R2 |
| **IN-5** | 能装能跑的最小公开面：包名、安装形式、最小 API、README、CI、版本号 | D1 已定 = 开源发布 |

### 3.3 v0.1 排除（OUT）

| # | 不做什么 | 为什么 |
|---|---|---|
| OUT-1 | 训练 / 微调 / 探针 | `AGENT.md` §3；违"低开销" |
| OUT-2 | 依赖模型隐状态的检测 | §5.6 白盒依赖无法用于闭源 API |
| OUT-3 | 向量库 / 索引 / 检索器 | `AGENT.md` §3；`site/index.md` §2 已公开承诺 |
| OUT-4 | 服务 / UI / 多租户 | D4 = library |
| OUT-5 | 多智能体知识状态委托与合并 | §6.3 R5，报告自评"理论攻坚"，G8 难度=高 |
| OUT-6 | AGM 式信念撤销 / 冲突仲裁 | §6.3 R1，研究机会而非 v0.1 工程 |
| OUT-7 | claim 级 provenance 抽取 | 需持续人工维护，违"低开销" |
| OUT-8 | 自动标注"应当弃权" | 团队已接受**合成样例集**；须守 §5.2 底线 |
| OUT-9 | 企业能力：审计 / 权限 / 责任归属 / 合规 | D2 = 开源开发者；属 C7 组织决断 |
| OUT-10 | 多语言实现（TS / Rust 绑定） | D3 = Python 单语言；schema 语言中立 |
| OUT-11 | 持久化知识状态的数据库 | 薄位定义使然；落库即引运维与多租户 |
| OUT-12 | 元数据的人工维护流程 | §4.3"没人会手工维护" |
| OUT-13 | 自称 AGI / 意识 / 通用自知 | `AGENT.md` §7 + `site/index.md` §2 双重禁止 |
| OUT-14 | 生成本身 | 判决先于生成（§2.5）；生成由调用方的 LLM 负责 |

### 3.4 边界之外但非排除（"以后"）

过时检测与复审提示（IN-1 的 `获取时间` 已留位）· service 薄壳（D4 建议 v1.1）· 双时态与冲突信号（C4，需 schema 稳定后）。

### 3.5 三条硬性红线

1. **评价优先**——无可复现指标的组件不进发布。
2. **低开销**——provenance 不得占掉可观的查询预算比例（`site/index.md` §3 原话）。
3. **诚实的不确定性**——`THINKING.md` 明列"把判断包装成事实"为最不可接受行为。

---

## §4 技术栈

### 4.1 已定（由既有决策推出）

| 项 | 结论 | 依据 |
|---|---|---|
| 实现语言 | Python | D3 建议 |
| 契约载体 | **语言中立的 JSON Schema + 规范文档**，Python 类型是其绑定 | D3 建议 |
| 交付形态 | library（**薄位待 §1 裁决**） | D4 已正式确认 |
| 仓库 | 单仓 | D6 建议 |
| 目标用户 | 开源开发者 | D2 已正式确认 |

### 4.2 待确认选型

**T1 运行时版本**
- `[FACT]` 本机：Python **3.14.3**（`py -0p` 默认）与 uv 托管 **3.11.15**；uv **0.12.1**、Node 24.13.0、git 2.52.0。
- 选项：≥3.12 / ≥3.11 / ≥3.13–3.14。
- **建议（非决策）**：`requires-python = ">=3.12"`。v0.1 不需 3.13/3.14 新特性；3.12 是"新到不别扭、老到能装上"的平衡点。本地 3.14 作 **额外** CI 矩阵项。

**T2 schema 定义方式（唯一需新依赖的决策）**
- 选项：A `pydantic` v2（最新 v2.13 线）/ B dataclasses + 标准库 `json`（零依赖）/ C `attrs` / `msgspec`。
- **建议（非决策）**：**A**。唯一能同时满足"schema 语言中立"与"低维护"的方案，且是 RAG/agents 生态默认存在。若坚持核心零依赖则 B 可行，代价是手写校验与 JSON Schema 同步。**团队决定，见 §5 问题 3。**

**T3 配置与构建**
- **建议（非决策）**：`pyproject.toml` 单一配置源（PEP 621 + `[build-system]`），后端 **hatchling**，环境/锁用本地已装的 **uv**。不再引 `setup.py` / `requirements.txt` 双轨。

**T4 强制依赖清单**
```
runtime : pydantic      # schema 定义 + 校验 + JSON Schema 生成
```
| 需要的能力 | 为什么不需要第三方 |
|---|---|
| 时间 / 时效 | 标准库 `datetime` + `zoneinfo` |
| 判决规则 | 纯条件逻辑 |
| 评价指标 | 纯算术，`math` 足够 |
| 序列化 | pydantic 或 `json` |

- **建议（非决策）**：嵌入模型、LLM 客户端、向量库、`numpy` **一律不进强制依赖**，只作 `[project.optional-dependencies]`，v0.1 可一个都不提供。这是"低开销"在依赖图上的落地。

**T5 测试与质量**
- **建议（非决策）**：`pytest`（+ `pytest-cov`）；`ruff` 统一 lint + format；类型检查 `pyright`（配置更少）。全部 dev 依赖，不进运行时。

**T6 评价层位置**
- **建议（非决策）**：自带指标实现（`eval/` + CLI + Markdown 报告），不引 `ragas` 等。理由：现成 RAG 评测框架不直接覆盖"弃权 precision/recall"（§6.3 R2 的论证）；为零存在指标引大型依赖违反"不重复造轮子"的**本意**。

**T7 文档站**
- `[FACT]` `site/index.md` 用 YAML front matter + 末尾内嵌 `<style>`；视觉基准为 `D:\github projects\Lemma-AI-Projects.github.io`。
- `[OPEN]` **未知**：团队用哪个渲染器（Jekyll / Hugo / 11ty / 自定义）。选错会让 front matter 以纯文本泄漏（风险已记录于 `2026-09-12.md`）。`.nojekyll` 的存在提示可能不使用 Jekyll 处理。
- **建议（非决策）**：v0.1 **不引入文档站构建器**，`site/index.md` 先按 Markdown 直接托管；加第二页时再定。

### 4.3 建议的仓库布局（**仅为形态示意，待确认后才创建**）

```
spec/                  # 语言中立契约：JSON Schema + 规范文档
src/autognose/         # Python 实现
tests/                 # 单元测试（合成证据 → 动作）
eval/                  # 评价集 + 指标 + 报告
site/                  # 现有 site/index.md 与 index.html
```

---

## §5 待裁决清单

| # | 问题 | 状态 |
|---|---|---|
| 1 | **薄位还是厚位**（§1） | `[OPEN]` **最高优先，决定其余全部** |
| 2 | D5 字段名与粒度：四字段是否就用「来源类型 / 获取时间 / 验证状态 / 任务边界」，粒度确认 chunk 级 | `[OPEN]` 公开 API 前置 |
| 3 | 是否接受 `pydantic` 为唯一运行时依赖（T2） | `[OPEN]` |
| 4 | T1 运行时下限 `>=3.12` 是否可接受 | `[OPEN]` |
| 5 | `BOUNDARY_REPORT.md` 与 `SCOPE.md` 是否进版本控制 | `[OPEN]` |

### 团队已决（本轮之前）

| 决策 | 值 | 日期 |
|---|---|---|
| D1 首个里程碑 | 开源发布 | 2026-09-11 |
| D2 目标用户 | 开源开发者 | 2026-09-12 |
| D4 交付形态 | library | 2026-09-12 |
| 弃权标注样例 | 无现成标注；**接受合成样例集开工** | 2026-09-12 |

---

## §6 合成样例集的三条底线（因 OUT-8 而产生）

`[JUDGMENT]` 若"应当弃权"由 LLM 或规则合成，而判决器用同一套逻辑，评价指标衡量的只是"我们的规则能否复现我们的合成规则"——**指标会好看且无意义**。三条底线：

1. 生成器与判决器**不得共享代码路径**（否则指标是恒等式）。
2. 每条样例必须带**可核验的"为什么该弃权"的理由**，而非只给标签。
3. 文档与指标输出**始终显式标注**"合成 / 未验证"；v0.1 **不宣称**任何对外可比的性能数字。

---

## §7 本文件自身的限制

- `[FACT]` 基于仓库内 10 个文件（含 3555 行调研报告全文）与 4 次外部检索，**未与团队任何人对话**。
- `[ASSUMPTION]` 假定团队以中文协作（调研报告与既有边界报告均为中文）。
- `[JUDGMENT]` §2 的生态位结论**逐条来自报告原文**；§1、§3 的划分依赖 D1–D4 既定决策。
- `[JUDGMENT]` §4 选型均可在确认后低成本更改，**除 T2**（schema 是公开契约，改动外溢到用户代码）。
- 本文件**未写任何代码、未创建目录树、未添加依赖**，遵守 `AGENT.md` §1/§7。
